export default function Manifest() {
  return (
    <section className="dr-manifest">
      <div className="dr-reveal dr-eyebrow-row">
        <span className="dr-eyebrow">[ 00 — Manifest ]</span>
      </div>
      <p className="dr-reveal dr-manifest-text">
        Wir sind kein Label. <span className="dr-accent">Wir sind eine Bande.</span> Drei Bands,
        eine Crew und ein Anspruch: ehrlich, laut und mit jeder Note ernst gemeint.
      </p>
    </section>
  )
}
